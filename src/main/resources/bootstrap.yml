spring:
  application:
    name: auth-server
  cloud:
    config:
      uri: http://localhost:8888


---

spring:
  profiles: docker
  cloud:
     config:
        uri: http://192.168.59.103:8888

---

spring:
  profiles: default
  cloud:
     config:
        enabled: false
eureka:
  client:
    enabled: false